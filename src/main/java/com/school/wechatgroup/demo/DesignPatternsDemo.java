package com.school.wechatgroup.demo;

/**
 * 本项目中的设计模式清单（面试吹牛素材）
 *
 * ┌─────────────────┬──────────────────────────────────────┬──────────────────────┐
 * │ 设计模式         │ 本项目中                              │ 面试怎么讲            │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 策略模式         │ FileParserStrategy/CsvParser/         │ "文件解析支持 CSV     │
 * │ (Strategy)      │ ExcelParser                          │  和 Excel，新增格式   │
 * │                 │                                      │  只需加实现类"        │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 模板方法         │ AbstractAuthenticationToken           │ "认证流程固定（检查    │
 * │ (Template M.)   │ → UsernamePasswordAuthToken          │  状态→验证→返回），    │
 * │                 │ → RememberMeAuthenticationToken      │  具体验证逻辑由子类    │
 * │                 │                                      │  实现"               │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 建造者模式       │ HttpSecurity.authorizeRequests()     │ "用 DSL 流畅构建      │
 * │ (Builder)       │   .antMatchers("/api/**").permitAll() │  安全配置链，避免     │
 * │                 │   .formLogin(...).build()            │  大量构造参数"        │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 代理模式         │ AuthAspect @Around + CGLIB           │ "AOP 方法级权限校验   │
 * │ (Proxy)         │                                      │  不侵入业务代码"      │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 责任链模式       │ ProviderManager → DaoAuthProvider    │ "多个认证提供者链式    │
 * │ (Chain of Resp.)│    → RememberMeProvider              │  执行，找到匹配的就    │
 * │                 │                                      │  停止"               │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 工厂方法         │ @Bean 方法: passwordEncoder()        │ "Spring 容器管理对象   │
 * │ (Factory M.)    │   authenticationManager()            │  创建，依赖注入解耦"  │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 单例模式         │ 所有 @Service/@Component Bean        │ "Spring 默认单例，    │
 * │ (Singleton)     │                                      │  避免重复创建开销"    │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 观察者模式       │ HttpSessionListener                 │ "Session 创建/销毁     │
 * │ (Observer)      │   → SessionRegistry.removeSession()  │  事件自动通知注册表"  │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 适配器模式       │ BCryptPasswordEncoderAdapter         │ "将 Spring Security   │
 * │ (Adapter)       │   → 自定义 PasswordEncoder 接口      │  的 BCrypt 适配到     │
 * │                 │                                      │  我们的接口体系"      │
 * ├─────────────────┼──────────────────────────────────────┼──────────────────────┤
 * │ 装饰器模式       │ VirtualFilterChain                  │ "包装原始 FilterChain │
 * │ (Decorator)     │   → 在原链前后插入自定义 Filter      │  扩展过滤逻辑"        │
 * └─────────────────┴──────────────────────────────────────┴──────────────────────┘
 */
public class DesignPatternsDemo {
    // 纯文档类，无运行时代码
}
